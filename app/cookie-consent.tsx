"use client";
import {useEffect,useState} from "react";
export default function CookieConsent(){const [show,setShow]=useState(false);useEffect(()=>setShow(!localStorage.getItem("tgc-consent")),[]);if(!show)return null;const save=(v:string)=>{localStorage.setItem("tgc-consent",v);window.dispatchEvent(new Event("tgc-consent"));setShow(false)};return <aside className="cookie" role="dialog" aria-label="Preferencias de privacidad"><div><b>Tu privacidad, sin letra chica.</b><p>Usamos lo esencial para que el sitio funcione. La medición y la publicidad personalizada permanecen desactivadas hasta que las aceptes.</p></div><div><button onClick={()=>save("essential")}>Solo necesarias</button><button className="accept" onClick={()=>save("accepted")}>Aceptar opcionales</button><a href="/privacidad">Ver detalles</a></div></aside>}
